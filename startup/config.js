import dotenv from "dotenv";
// import morgan from 'morgan'
// import helmet from 'helmet'
//import winstone from "winstone";

export default (app) => {
  dotenv.config();
  //winstone.add(winstone.transports.File, { filename: "logfile.log" });
  // app.set("view engines", "pug");
  // app.set("views", "./views");
};
